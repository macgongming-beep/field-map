# 보안 쓰기 정책 교체 지도

운영 DB 읽기 전용 실측: 2026-09-07 15:59:19 KST. 이 문서는 `npm run audit:security-transition`으로 다시 만들 수 있다.

## 범위와 불변 조건

- TEMP 쓰기 정책: **6칸** · 표 **2개** · DELETE 2 · INSERT 2 · UPDATE 2
- 같은 표·명령의 TEMP/최종 쓰기 정책 공존: **0칸**
- 이번 전환은 INSERT·UPDATE·DELETE만 다룬다. SELECT 정책은 Realtime 계약 때문에 변경하지 않는다.
- 함수 연결은 PL/pgSQL 본문의 정적 INSERT·UPDATE·DELETE·TRUNCATE 참조다. 동적 SQL과 간접 호출은 수동 검토가 필요하다.
- 트리거 함수는 직접 RPC 호출 대상은 아니지만, 해당 표 쓰기의 부수 효과이므로 목록에 남긴다.

## 활성 승인 역할

| 역할 | 인원 |
|---|---:|
| `admin` | 7 |
| `developer` | 1 |
| `leader` | 17 |
| `user` | 40 |

각 묶음 적용 전 이 분포와 실제 화면 노출 역할을 다시 대조한다.

## 교체 순서

1. 파일럿: `app_settings` 3칸 + `notices` INSERT·DELETE 2칸. 현재도 관리자 검사식이라 동작 변경 없이 이름과 검증 계약만 최종화한다.
2. 일정·배정: `calendar_events`, `event_participants`, `event_card_assignments`, `event_card_assignment_cards`, `card_assignments`, `card_leader_assignments`.
3. 개인 봉사 자료: `service_sessions`, `regular_visits`, `return_visits`, `return_visit_logs`.
4. 구역 핵심: `cards`, `card_boundaries`, `buildings`, `units`, `visit_histories`.
5. `app_users`: 역할 상승과 definer 우회를 함께 다루는 마지막 단독 묶음.

각 묶음에는 preflight, 역할 매트릭스, 관련 definer 계약, 실제 앱 저장 확인, rollback을 함께 둔다.

## TEMP 정책 6칸

| 표 | 명령 | 정책 | 대상 역할 | USING | WITH CHECK |
|---|---|---|---|---|---|
| `app_users` | DELETE | `TEMP_session_gate_app_users_del` | `anon` | `( SELECT private.request_is_admin() AS request_is_admin)` | - |
| `app_users` | INSERT | `TEMP_session_gate_app_users_ins` | `anon` | - | `( SELECT private.request_is_admin() AS request_is_admin)` |
| `app_users` | UPDATE | `TEMP_session_gate_app_users_upd` | `anon` | `((id = ( SELECT private.request_session_user_id() AS request_session_user_id)) OR ( SELECT private.request_is_admin() AS request_is_admin))` | `((id = ( SELECT private.request_session_user_id() AS request_session_user_id)) OR ( SELECT private.request_is_admin() AS request_is_admin))` |
| `visit_histories` | DELETE | `TEMP_session_gate_visit_histories_del` | `anon` | `(( SELECT private.request_session_user_id() AS request_session_user_id) IS NOT NULL)` | - |
| `visit_histories` | INSERT | `TEMP_session_gate_visit_histories_ins` | `anon` | - | `(( SELECT private.request_session_user_id() AS request_session_user_id) IS NOT NULL)` |
| `visit_histories` | UPDATE | `TEMP_session_gate_visit_histories_upd` | `anon` | `(( SELECT private.request_session_user_id() AS request_session_user_id) IS NOT NULL)` | `(( SELECT private.request_session_user_id() AS request_session_user_id) IS NOT NULL)` |

## 연결된 SECURITY DEFINER 쓰기 후보

| 표 | 함수 | 작업 | anon 실행 | 종류 |
|---|---|---|---|---|
| `app_users` | `public.auth_login(p_login_id text, p_pin text, p_device_label text, p_user_agent text)` | UPDATE | 예 | 함수 |
| `app_users` | `public.auth_record_auto_login(p_user_id integer, p_device_label text, p_user_agent text)` | UPDATE | 예 | 함수 |
| `app_users` | `public.signup_tx(p_login_id text, p_name text, p_pin text)` | INSERT | 예 | 함수 |
| `visit_histories` | `private.register_restaurant_core(p_actor_name text, p_name text, p_address text, p_existing_building_id integer, p_card_id integer, p_lat double precision, p_lng double precision, p_is_chinese boolean, p_initial_state text, p_regular_visitor text, p_visited_at timestamp with time zone, p_memo text, p_building_name text)` | INSERT | 아니오 | 함수 |
| `visit_histories` | `public.delete_old_visit_histories(cutoff_date text)` | DELETE | 예 | 함수 |
| `visit_histories` | `public.import_building_tx(p_token uuid, p_building jsonb, p_units jsonb)` | INSERT | 예 | 함수 |
| `visit_histories` | `public.rename_user_name_references(p_token uuid, p_old text, p_new text)` | UPDATE | 예 | 함수 |

## 동적 SQL 수동 검토

아래 definer는 본문에 동적 SQL이 있어 위 정적 연결표만으로 안전 판정을 내리지 않는다.

- `public.get_comment_target_author_id(p_target_type text, p_target_id integer)` · anon 실행 가능 · 반환 `integer`

## 묶음별 필수 계약

- 토큰 없음 쓰기 차단
- 일반 사용자의 허용 범위와 타인 자료 차단
- 인도자와 관리자·개발자의 실제 업무 범위 허용
- 연결된 definer 함수도 같은 역할·소유권 계약 준수
- 불필요한 anon/PUBLIC execute 회수. 공개 로그인 함수와 트리거는 별도 근거 기록
- SELECT 정책과 Realtime 수신 동작 불변
- 최근 백업 확인, preflight, 적용, 실제 앱 저장, rollback 순서 보장

